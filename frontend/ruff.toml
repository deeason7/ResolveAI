# Same lint standard as backend/pyproject.toml — one style across the repo.
line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "I", "UP", "B", "C4", "PIE"]
ignore = ["E501"]

[format]
quote-style = "double"
